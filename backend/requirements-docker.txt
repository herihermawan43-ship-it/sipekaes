# Minimal dependency set actually used by server.py / auth.py / seed.py.
# The main requirements.txt carries a much larger set inherited from the
# Emergent platform template (unused AI SDKs, etc.) — this file is for
# lean production Docker builds instead.
fastapi==0.110.1
uvicorn[standard]==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic==2.13.4
python-jose==3.5.0
bcrypt==4.1.3
openpyxl==3.1.5
python-dotenv==1.2.3
python-multipart==0.0.32
